services:
  marquinhosbot:
    command: bun run dev
    env_file: !reset []
    volumes:
      - ./.env:/app/.env:ro
      - ./src:/app/src:ro
      - ./tsconfig.json:/app/tsconfig.json:ro
    environment:
      - NODE_ENV=development
    extra_hosts:
      - "host.docker.internal:host-gateway"
