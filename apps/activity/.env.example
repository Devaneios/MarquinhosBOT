# Public — bundled into the client, must match the Application ID in the
# Discord Developer Portal.
VITE_DISCORD_CLIENT_ID=

# marquinhos-api origin used for local browser iteration (outside Discord's
# proxy). Not used when running inside the Discord client — see README.
VITE_API_ORIGIN=http://localhost:3000
